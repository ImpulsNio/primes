// ==================================================
// primes.hpp
// ==================================================

#include <iostream>

class Primes
{
public:
  static bool is_prime(const unsigned int NUM);
  static bool is_cyclic(const unsigned int PRIME);
};
